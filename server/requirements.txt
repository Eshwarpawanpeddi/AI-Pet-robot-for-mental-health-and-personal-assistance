# FastAPI for backend REST API (Python 3.7+)
fastapi==0.109.1

# Uvicorn for running FastAPI apps
uvicorn[standard]==0.27.0

# WebSocket library (aligned with FastAPI and uvicorn compatibility)
websockets==12.0

# File upload support for FastAPI
python-multipart==0.0.18

# Google Generative AI API (compatible version with resolved pydantic conflicts)
google-generativeai==0.8.3

# dotenv for environment variable management
python-dotenv==1.0.0

# Pydantic compatibility for model validation (Python 3.7+)
pydantic==2.5.3
pydantic-settings==2.1.0

# Async file handling for FastAPI
aiofiles==23.2.1

# Async HTTP client support (Python 3.7+)
aiohttp==3.9.1

# Computer vision and ML for emotion detection
opencv-python==4.8.1.78
tensorflow==2.15.0
numpy==1.24.3

# YOLO object detection for autonomous navigation
ultralytics==8.0.196